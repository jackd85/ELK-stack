input {
    file {
        path => "/opt/logstash/output.txt"
        start_position => "beginning"
    }
    http {

    }
    
}

filter {
    grok {
        match => { "message" => "%{HTTPD_COMBINEDLOG}" }
    }

    mutate {
        gsub => [
            "agent", '"', "",
            "referrer", '"', ""
        ]
    }
}

output {
    stdout {
        codec => rubydebug
    }
}